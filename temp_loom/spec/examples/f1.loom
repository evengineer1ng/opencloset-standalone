# Two fields -> one .oradio. A finished race is an imported tape; the engine just replays it.
universe: The Barcelona Grand Prix, replayed lap by lap — a telemetry department reading the race as it unfolds, deterministic and repeatable.
connections:
  - plugin: tape_replay
    name: trackside
    path: data/f1_barcelona_2026.json
