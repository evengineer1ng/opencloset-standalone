# Core dependencies for Radio OS
# Install with: pip install -r requirements.txt

# Audio
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0
pygame>=2.5.0                       # Multi-channel audio mixing (music/sfx)

# UI
# tk is usually included in standard Python. On Linux: sudo apt-get install python3-tk
Pillow>=9.0.0
customtkinter>=5.2.0                # Modern themed UI components

# Data
pyyaml>=6.0
requests>=2.31.0
feedparser>=6.0.10
pydantic>=2.9.1                     # EDAP EDMesg bridge payloads
pyzmq>=26.2.0                       # EDAP EDMesg bridge transport

# Mac Media Control (optional, Mac only)
pyobjc-framework-MediaPlayer>=9.0; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=9.0; sys_platform == 'darwin'       # provides AppKit, Foundation, etc.

# Windows Media Control (optional, Windows only)
winsdk>=1.0.0b10; sys_platform == 'win32'

# Visual Reader Plugin (optional, for visual content analysis)
# Install required for ANY visual_reader use:
mss>=1.8.0                          # Fast screen capture
pyautogui>=0.9.53                   # Fallback screen/window capture
opencv-python-headless>=4.8.0          # Video frame extraction (headless avoids SDL2 conflict with pygame)
pygetwindow>=0.0.9; sys_platform == 'win32'  # Robust window enumeration (Windows)

# Optional plugin dependencies (install as needed)
pydub>=0.25.1                       # Audio manipulation/editing (requires ffmpeg)
imageio>=2.31.0                     # Image/video I/O (video_timeline, etc.)
SpeechRecognition>=3.10.0           # Speech-to-text (if using audio input plugins)
faster-whisper>=1.2.0               # Priority-0 STT engine; pure-Python CTranslate2 (ARM64-compatible)
matplotlib>=3.7.0                   # Visualization/plotting (analysis widgets)

# iRacing Live Commentary Station (Windows only for real data; sim mode on all platforms)
pyirsdk>=1.3.0; sys_platform == 'win32'   # iRacing shared-memory SDK Python wrapper
# Web Server (Radio OS Web Shell — optional, for Tailscale/LAN access)
fastapi>=0.100.0                    # Web API framework
uvicorn>=0.23.0                     # ASGI server
httpx>=0.24.0                       # Async HTTP client (station web proxy)
websockets>=11.0                    # WebSocket client (station WS proxy)

# Vision providers (optional, install ONE):
openai>=1.3.0                       # For OpenAI GPT-4-Vision / GPT-4o
anthropic>=0.7.0                    # For Anthropic Claude
google-generativeai>=0.3.0          # For Google Gemini

# Windows-only (optional, for window capture):
pywin32>=305; sys_platform == 'win32'

# Kokoro TTS (local ONNX-based TTS — requires model files in voices/kokoro/)
# Model files: kokoro-v1.0.onnx + voices-v1.0.bin (~338MB total)
# Download from: https://github.com/thewh1teagle/kokoro-onnx/releases/tag/model-files-v1.0
kokoro-onnx>=0.5.0

# Optional: ML features for From the Backmarker station (~2GB download)
# Gracefully degrades to simpler AI if not installed
# Install if prompted during setup, or manually: pip install torch>=2.0.0
# torch>=2.0.0
zeroconf>=0.131
